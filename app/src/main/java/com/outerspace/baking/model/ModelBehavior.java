package com.outerspace.baking.model;

public enum ModelBehavior {
        NETWORK_REQUEST,
        MOCK_NETWORK_ERROR,
        MOCK_EMPTY_LIST,
        MOCK_ONE_RECORD_LIST,
        MOCK_LONG_LIST,
}
